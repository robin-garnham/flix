{
  "name": "myflix-server",
  "version": "1.0.0",
  "description": "Simple server for myflix",
  "main": "./src/server.js",
  "scripts": {
    "start": "node ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Robin Garnham",
  "license": "ISC",
  "dependencies": {
    "restify": "^8.5.1",
    "restify-cors-middleware": "^1.1.1",
    "restify-router": "^0.5.1"
  }
}
